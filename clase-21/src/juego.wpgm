import wollok.game.*
import auto.*

program juego {
	game.cellSize(55)
	game.width(9)
	game.height(10)
	game.addVisual(auto)
	keyboard.right().onPressDo {
		auto.moveteADerecha()
	}
	keyboard.left().onPressDo {
		auto.moveteAIzquierda()
	}
	game.start()

}